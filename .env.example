# No API key needed. This app calls your locally-installed, logged-in
# Claude Code CLI (your Claude subscription). The variables below are OPTIONAL.

# Override the CLI location if it isn't auto-detected:
# CLAUDE_CLI_PATH=/Users/you/.npm-global/bin/claude

# Model to answer with (default: sonnet):
# CHAT_MODEL=sonnet
